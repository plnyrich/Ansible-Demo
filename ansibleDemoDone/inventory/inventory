[webservers]
# comment the following line if using vagrant machine
localhost

# uncomment the following line if using vagrant machine
#localhost ansible_ssh_user=vagrant ansible_ssh_pass=vagrant ansible_ssh_port=2222 ansible_become=yes
